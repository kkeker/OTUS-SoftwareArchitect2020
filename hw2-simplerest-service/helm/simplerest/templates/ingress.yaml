---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: '{{.Values.general.name}}-ingress'
  annotations:
    kubernetes.io/ingress.class: '{{.Values.ingress.class}}'
    nginx.ingress.kubernetes.io/rewrite-target: '{{.Values.ingress.nginxRewriteTarget}}'
    nginx.ingress.kubernetes.io/ssl-passthrough: '{{.Values.ingress.nginxEnableSsl}}'
spec:
  rules:
    - host: {{.Values.ingress.host}}
      http:
        paths:
          - path: {{.Values.ingress.path}}
            pathType: {{.Values.ingress.pathType}}
            backend:
              service:
                name: '{{.Values.general.name}}-service'
                port:
                  number: {{.Values.deployment.port}}